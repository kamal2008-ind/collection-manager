1. More menu component
2. Favorite morph table
3. Movie statistics and settings
4. Mobile view - No more workspaces. No more collections. no more movies. - message should be display at end of the page.
5. Partials folder - some files are as it in every module. Does not need it 4 files, need only 1 file to handle all modules.

SELECT id, migration
FROM migrations
WHERE migration LIKE '%book%';

DELETE FROM migrations
WHERE id IN (21, 22);